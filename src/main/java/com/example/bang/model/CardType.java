package com.example.bang.model;

public enum CardType {
    // Brown cards (immediate effect, then discard)
    BANG,
    MISSED,
    BEER,
    SALOON,
    STAGECOACH,
    WELLS_FARGO,
    PANIC,
    CAT_BALOU,
    DUEL,
    GATLING,
    INDIANS,
    GENERAL_STORE,

    // Blue cards (equipment - stays in play)
    BARREL,
    MUSTANG,
    SCOPE,
    JAIL,
    DYNAMITE,

    // Weapons (blue cards)
    VOLCANIC,
    SCHOFIELD,
    REMINGTON,
    REV_CARABINE,
    WINCHESTER
}
